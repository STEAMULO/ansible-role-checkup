---
# vars file for ansible-role-checkup

# vars for openSCAP
oval_filename: "oval.xml"
oval_url: "https://people.canonical.com/~ubuntu-security/oval/com.ubuntu.{{ ansible_distribution_release }}.cve.oval.xml"
oval_path: "/tmp/{{ oval_filename }}"
result_filename: "results-openscap.xml"
result_path: "/tmp/{{ result_filename }}"
report_filename: "report-openscap.html"
report_path: "/tmp/{{ report_filename }}"

